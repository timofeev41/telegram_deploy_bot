{
  "name": "example deployment schema",
  "target": "on 127.0.0.1",
  "chain": [
    { "action": "mkdir app", "description": "Create /app dir" },
    { "action": "echo '1234' > app/newfile.txt", "description": "Create test file" }
  ]
}
